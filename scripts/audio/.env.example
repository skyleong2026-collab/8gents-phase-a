# Copy this file to ".env" (same folder) and paste your key after the =
# Get a key in ElevenLabs: profile menu → API Keys → Create API Key.
# The real .env is gitignored — it is NEVER committed and NEVER shipped in the web app
# (this key is only used by the build-time SFX generator script).
ELEVENLABS_API_KEY=
